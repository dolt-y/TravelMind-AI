# TravelMind-AI 迁移清单

本文档记录 TravelMind-AI 旅行规划业务所需的功能、接口、数据和验收要求。
`[x]` 表示当前代码已经具备，`[ ]` 表示仍需实现或补齐。

## 当前已具备

- [x] 通过 Spider_XHS PC 客户端搜索小红书笔记。
- [x] 根据笔记 ID 读取详情，详情失败时保留搜索卡片内容。
- [x] 使用 LLM 从旅行笔记提取景点候选。
- [x] 将景点候选按城市和中文名称匹配高德 POI。
- [x] 补充 `poi_id`、`address`、`location`、`rating`、`photos`。
- [x] 将小红书原文、景点候选和 POI 结果保存到 SQLite。
- [x] 提供 POI 搜索、POI 详情和小红书景点图片接口。
- [x] 提供提取记录 ID 查询接口。
- [x] 终端输出小红书、LLM、POI 和 SQLite 的实时业务进度。
- [x] Cookie、`xsec_token`、LLM Key 和地图 Key 不写入业务数据表。

## 数据来源边界

后续实现必须保持以下职责划分：

| 数据或判断 | 责任方 | 约束 |
| --- | --- | --- |
| 游记正文、真实评价、预约提示 | 小红书 | 保存原文和来源记录，不保存 Cookie 或令牌 |
| 景点名称候选、游玩时长、推荐理由 | LLM | 只从笔记内容提取，输出必须经过模型校验 |
| POI ID、地址、经纬度、电话、评分 | 高德或 Google 地图 API | 不允许由 LLM 编造 |
| 天气温度和天气状况 | 天气 API | LLM 只能调用和整理，不能自行生成事实 |
| 酒店地点和基础信息 | 地图文本搜索 API | LLM 负责筛选和解释推荐理由 |
| 景点访问顺序 | Planner LLM | 结合用户偏好、停留时间和地点分布生成 |
| 两点距离、交通耗时、路线步骤 | 路线 API | 用于校验和展示，不由 LLM 编造 |
| 最终日期、预算和字段完整性 | 业务校验器 | 在返回客户端前执行确定性校验 |

## 天气查询

- [x] 增加天气领域模型，至少包含 `date`、`city`、白天/夜间天气、温度、风向和风力。
- [x] 定义地图 Provider 的天气接口，隔离高德 REST、Google Weather 或其他供应商的字段差异。
- [x] 实现高德天气查询，支持城市和日期范围参数，并明确天气预报的有效日期范围。
- [x] 解析高德错误状态、空预报和城市不存在等情况，返回可识别的业务错误。
- [x] 为天气查询增加 SQLite 缓存，缓存键至少包含供应商、城市和预报日期。
- [x] 增加 `GET /api/weather` 和兼容 TripStar 的 `GET /api/map/weather` 路由。
- [x] 在终端输出天气查询开始、供应商、返回天数、缓存命中和失败原因。
- [ ] 在 Planner 输入中保留每个城市的天气原始事实，禁止 LLM 在缺失时伪造温度。
- [x] 测试正常响应、空数据、API 错误和缓存命中。
- [ ] 测试 Planner 编排中的多城市天气查询。

## 酒店搜索

- [ ] 增加酒店领域模型，至少包含名称、地址、坐标、类型、评分、价格区间和图片。
- [ ] 定义地图 Provider 的酒店文本搜索接口，统一高德和 Google 的搜索结果结构。
- [ ] 支持按城市、住宿偏好、预算和景点区域构建搜索条件。
- [ ] 保存酒店搜索结果和查询条件，避免每次生成行程都重复请求相同城市。
- [ ] 增加酒店搜索业务接口，返回标准化酒店列表和来源信息。
- [ ] 在 Planner 输入中区分“API 返回的酒店事实”和“LLM 的推荐解释”。
- [ ] 对缺失价格、评分或地址的酒店保留空值，不用模型补造具体数值。
- [ ] 在终端输出酒店查询城市、关键词、结果数量、缓存状态和异常信息。
- [ ] 测试不同住宿偏好、无结果、供应商失败和多城市酒店查询。

## 路线和交通

- [ ] 明确路线分为两类：景点访问顺序由 Planner LLM 规划，两点之间的距离和耗时由路线 API 计算。
- [ ] 增加路线领域模型，至少包含起点、终点、交通方式、距离米数、耗时秒数、文本描述和步骤。
- [ ] 定义 Provider 路线接口，支持步行、驾车和公共交通，并保留供应商原始路线标识。
- [ ] 实现高德路线查询和结果解析，不能只返回 MCP 原始字符串。
- [ ] 保留 `POST /api/map/route` 兼容路径，并返回稳定的 `RouteResponse`。
- [ ] 为每日相邻景点生成路线段，保存路线段和查询时间，支持结果复用。
- [ ] 在最终行程返回前校验路线段的起点、终点和交通方式是否与行程一致。
- [ ] 在终端输出路线查询的起终点、交通方式、距离、耗时和失败原因。
- [ ] 测试路线 API 超时、无路线、跨城市路线和不同交通方式。

## 行程规划编排

- [ ] 增加 `TripRequest`，支持单城市、多城市、日期、停留天数、交通方式、住宿偏好、旅行偏好和额外要求。
- [ ] 增加 `TripPlan`、`DayPlan`、`Meal`、`Hotel`、`WeatherInfo`、`Budget` 等稳定领域模型。
- [ ] 将每个城市的采集结果汇总为 Planner 输入：小红书景点、POI 坐标、天气和酒店。
- [ ] 让 Planner LLM 负责每日景点顺序、餐饮建议、住宿选择和文字说明。
- [ ] 让确定性服务负责日期覆盖、城市归属、景点坐标、路线段和预算求和。
- [ ] 对 LLM 返回的 JSON 做代码块清理、字段校验、数值校验和缺失字段处理。
- [ ] 对无效计划执行一次可控重试；重试仍失败时保存失败原因，不返回半成品计划。
- [ ] 支持多城市移动日，明确城市切换、交通建议和当日景点数量。
- [ ] 景点图片继续由图片接口按需查询，不把图片抓取阻塞在主规划请求中。
- [ ] 终端按城市输出景点、天气、酒店、规划和校验进度。

## 异步任务和状态

- [ ] 增加 `POST /api/trip/plan`，提交后立即返回 `task_id`。
- [ ] 增加 `GET /api/trip/status/{task_id}`，返回状态、进度、当前业务消息和最终结果。
- [ ] 增加 `WS /api/trip/ws/{task_id}`，支持前端实时订阅任务状态。
- [ ] 任务状态至少包含 `submitted`、`processing`、`completed`、`failed`。
- [ ] 每次状态变化都保存到持久化存储，服务重启后可以查询已提交任务。
- [ ] 任务失败时区分配置错误、认证失败、供应商网络错误、LLM 错误和数据校验错误。
- [ ] 防止同一任务被重复执行或重复写入最终结果。
- [ ] 终端日志中的 `task_id` 只用于关联业务进度，不打印请求密钥或完整用户隐私内容。

## 持久化和查询

- [ ] 增加旅行任务、完整行程、每日计划和路线段数据表。
- [ ] 建立 `task_id`、`plan_id`、城市和创建时间索引。
- [ ] 保存原始供应商响应前先删除 Cookie、`xsec_token`、API Key 和 Authorization 字段。
- [ ] 为天气、酒店和路线结果设置查询时间，支持过期重新获取。
- [ ] 增加历史行程列表和按 `plan_id` 查询完整行程接口。
- [ ] 保证任务记录、行程记录和路线段记录的事务一致性。
- [ ] 为已有 SQLite 数据库提供增量表结构升级，不破坏已有小红书和 POI 数据。

## 前端 暂时不用实现（最后处理）

- [ ] 增加旅行规划输入页，支持城市、日期、偏好、交通和住宿配置。
- [ ] 增加任务进度页，展示当前城市和当前业务动作。
- [ ] 增加行程结果页，展示每日景点、酒店、餐饮、天气和预算。
- [ ] 接入地图 SDK，根据已校验的 POI 坐标绘制标记和路线。
- [ ] 按需加载景点小红书图片，图片失败时显示明确占位状态。
- [ ] 增加历史行程入口，支持从 `plan_id` 恢复查看。
- [ ] 增加行程上下文 AI 问答，并限制回答范围为当前已生成的行程数据。

## 配置和安全

- [ ] 增加天气、酒店和路线 Provider 所需的环境变量说明。
- [ ] 明确高德 Web 服务 Key、Web JS Key 和 Google Key 的用途边界。
- [ ] 所有真实 Cookie 和 API Key 只放在 `.env` 或运行时安全配置中。
- [ ] 健康检查只返回配置是否存在，不返回密钥内容或可逆掩码。
- [ ] 日志、异常消息和持久化记录不得包含 Cookie、`xsec_token`、Authorization 或完整 LLM 请求。
- [ ] 对外接口统一返回可识别的错误结构，避免把供应商完整响应直接暴露给客户端。

## 测试和验收

- [ ] 为高德、Google、天气、酒店和路线 Provider 编写假响应测试。
- [ ] 为小红书提取到 POI 补全的完整链路编写集成测试。
- [ ] 验证 SQLite 保存、历史读取、缓存命中、旧表升级和事务回滚。
- [ ] 验证 LLM 返回纯 JSON、Markdown JSON、额外说明、空数组和非法字段。
- [ ] 验证供应商超时、无 Key、Cookie 过期、网络不可达和限流场景。
- [ ] 验证单城市、多城市、移动日和日期边界。
- [ ] 验证 API 文档中的路径、方法、请求字段和响应字段与前端调用一致。
- [ ] 验证终端日志能覆盖成功、降级和失败路径，且不含敏感信息。
- [ ] 运行编译检查、依赖锁检查和完整测试后，再接入真实高德、LLM 和小红书请求。
